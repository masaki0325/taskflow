# ============================================================
# .dockerignore - Dockerビルド時に除外するファイル
# ============================================================
# .gitignore と似た機能
# COPY . . でコピーする際に、これらのファイルは除外される
# → イメージサイズ削減、ビルド時間短縮、セキュリティ向上

# ------------------------------------------------------------
# Pythonのキャッシュファイル
# ------------------------------------------------------------
# これらは環境ごとに異なるので、コンテナに含めない
__pycache__          # Pythonのバイトコードキャッシュディレクトリ
*.pyc                # コンパイル済みPythonファイル
*.pyo                # 最適化されたPythonファイル
*.pyd                # Pythonの動的リンクライブラリ

# ------------------------------------------------------------
# テスト関連
# ------------------------------------------------------------
.pytest_cache        # pytestのキャッシュ
.coverage            # カバレッジ測定の結果ファイル
htmlcov              # カバレッジレポート（HTML）

# ------------------------------------------------------------
# ビルド成果物
# ------------------------------------------------------------
dist                 # ビルドされたパッケージの出力先
build                # ビルドの一時ファイル
*.egg-info           # Pythonパッケージのメタデータ

# ------------------------------------------------------------
# 環境変数ファイル
# ------------------------------------------------------------
# .env には機密情報（パスワード、APIキーなど）が含まれる
# イメージに含めると情報漏洩のリスクがあるため除外
.env                 # 環境変数ファイル

# ------------------------------------------------------------
# 仮想環境
# ------------------------------------------------------------
# ローカルの仮想環境はコンテナに不要
# コンテナ内で pip install するため
.venv                # 仮想環境ディレクトリ (python -m venv .venv)
venv                 # 仮想環境ディレクトリ (古い命名規則)
env                  # 仮想環境ディレクトリ

# ------------------------------------------------------------
# Git関連
# ------------------------------------------------------------
# Gitの履歴はコンテナに不要、イメージサイズを増やすだけ
.git                 # Gitリポジトリ（数百MB〜数GBになる可能性）
.gitignore           # Git設定ファイル

# ------------------------------------------------------------
# ドキュメント
# ------------------------------------------------------------
# 開発用ドキュメントは本番環境に不要
README.md            # プロジェクトのREADME

# ============================================================
# なぜこのファイルが重要か？
# ============================================================
# 1. イメージサイズ削減
#    .git ディレクトリだけで数百MBになることも
#    不要ファイルを除外することで、イメージが小さく軽快に
#
# 2. ビルド時間短縮
#    コピーするファイルが少ないほど、ビルドが速い
#
# 3. セキュリティ
#    .env ファイルがイメージに含まれると、機密情報が漏洩する
#
# 4. 一貫性
#    ローカル環境のキャッシュや設定が混入しない
# ============================================================
