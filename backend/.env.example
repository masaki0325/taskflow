# Database
# PostgreSQL接続URL（形式: postgresql://ユーザー名:パスワード@ホスト:ポート/データベース名）
DATABASE_URL=postgresql://taskflow_user:taskflow_pass@localhost:5432/taskflow_db

# Security
# JWT署名用の秘密鍵（本番環境では必ず変更すること！ランダムな文字列を生成）
SECRET_KEY=your-secret-key-here-please-change-in-production
# JWT署名アルゴリズム（HS256 = HMAC-SHA256）
ALGORITHM=HS256
# Access Token の有効期限（分）- 短命にすることでセキュリティを向上
ACCESS_TOKEN_EXPIRE_MINUTES=15
# Refresh Token の有効期限（日）- Access Token の再発行に使用
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS
# クロスオリジンリクエストを許可するオリジンのリスト（カンマ区切り）
# 例: Next.jsフロントエンド(3000), FastAPI自体(8000)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# Redis
# Redis接続URL（セッション・キャッシュ管理用）
REDIS_URL=redis://localhost:6379/0

# Environment
# 実行環境（development, staging, production）
ENVIRONMENT=development
